name: Manual Release

on:
  workflow_dispatch:
    inputs:
      plugin:
        description: 'Select plugin to release'
        required: true
        type: choice
        options:
          - risu-btw-plugin
          - risu-editor-plugin
      release_type:
        description: 'Release type'
        required: true
        type: choice
        default: 'beta'
        options:
          - stable
          - beta
      custom_version:
        description: 'Custom version string (optional, e.g. 1.1.0. Leave empty to use package.json version)'
        required: false
        type: string

jobs:
  build-and-release:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'
          cache-dependency-path: '**/pnpm-lock.yaml'

      - name: Install dependencies
        working-directory: ${{ github.event.inputs.plugin }}
        run: |
          if [ -f pnpm-lock.yaml ]; then
            pnpm install --frozen-lockfile --dangerously-allow-all-builds
          else
            pnpm install --no-frozen-lockfile --dangerously-allow-all-builds
          fi

      - name: Generate version and tag
        id: versioning
        working-directory: ${{ github.event.inputs.plugin }}
        run: |
          VERSION="${{ github.event.inputs.custom_version }}"
          if [ -z "$VERSION" ]; then
            VERSION=$(jq -r .version package.json)
          fi

          # If beta release and version doesn't already contain a prerelease suffix, add beta suffix to VERSION
          if [ "${{ github.event.inputs.release_type }}" = "beta" ] && [[ ! "$VERSION" =~ - ]]; then
            VERSION="${VERSION}-beta"
          fi

          TAG="${{ github.event.inputs.plugin }}@v${VERSION}"

          # Update package.json version
          jq --arg ver "$VERSION" '.version = $ver' package.json > package.json.tmp && mv package.json.tmp package.json

          echo "version=${VERSION}" >> $GITHUB_OUTPUT
          echo "tag=${TAG}" >> $GITHUB_OUTPUT
          echo "Generated tag: ${TAG}"

      - name: Build
        working-directory: ${{ github.event.inputs.plugin }}
        run: pnpm build

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.versioning.outputs.tag }}
          name: ${{ steps.versioning.outputs.tag }}
          body: |
            Manual ${{ github.event.inputs.release_type }} release of `${{ github.event.inputs.plugin }}` v${{ steps.versioning.outputs.version }}
          files: ${{ github.event.inputs.plugin }}/dist/*
          prerelease: ${{ github.event.inputs.release_type == 'beta' }}
          make_latest: ${{ github.event.inputs.release_type == 'stable' }}
